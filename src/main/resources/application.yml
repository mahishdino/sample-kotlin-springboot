spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    driverClassName: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
  application:
    name: todo
  graphql:
    graphiql:
      enabled: true
      path: /graphiql
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
